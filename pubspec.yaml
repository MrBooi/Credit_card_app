name: card_app
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # StateManagement and DI
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  flutter_bloc: ^8.1.3
  # dependency injection
  get_it: ^7.6.4
  # path
  path_provider: ^2.1.1
  # credit card scanner
  flutter_scan_tools: ^0.0.3
  # permission_handler
  permission_handler: ^11.0.0

  #localStorage
  hive: ^2.2.3

  # functional programming
  dartz: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3

  # code gen
  freezed: ^2.4.2
  json_serializable: ^6.7.1
  build_runner: ^2.4.6

flutter:
  uses-material-design: true
